# OS files
.DS_Store
Thumbs.db

# Logs
*.log

# Secrets — never commit these
.env
*.env

# Claude Code project file (not part of the website)
CLAUDE.md

# WordPress sensitive files — NEVER commit these
blog/wp-config.php
blog/wp-content/uploads/
blog/.htaccess

# Node (if ever used)
node_modules/
